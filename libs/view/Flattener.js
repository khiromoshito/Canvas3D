import { Model3D } from "../schemas/Model3D.js";

export var Flattener = {

    /**
     * Flattens a model and returns a `StreamData2D`
     * @param {Model3D} model
     */
    flattenModel: (model) => {
        
    }
}